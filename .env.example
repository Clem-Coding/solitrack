# ====================
# ENVIRONMENT CONFIGURATION
# ====================

# Define the execution environment (dev, prod, etc.)
APP_ENV=dev

# ====================
# DATABASE CONFIGURATION
# ====================

# For XAMPP with MySQL (local)
# DATABASE_URL="mysql://root:root@127.0.0.1:3306/database_name"

# If you're using PostgreSQL, uncomment the line below:
# DATABASE_URL="pgsql://username:password@127.0.0.1:5432/database_name"

# ====================
# MAILER CONFIGURATION (if you're using a mail service)
# ====================

# Example with an SMTP service
# MAILER_DSN="smtp://username:password@smtp.example.com:587"

# If you're using an SMTP service like Mailgun, SendGrid, or another, uncomment this line:
# MAILER_DSN="smtp://username:password@smtp.example.com:587"

# Example configuration for Mailtrap (in development environment)
# Uncomment the line below if you're using Mailtrap to test email sending:
# Replace "username" and "password" with the credentials provided by Mailtrap
# MAILER_DSN="smtp://username:password@smtp.mailtrap.io:2525"
