{# {% extends 'base.html.twig' %}

{% block title %}
  Hello VolunteerSessionController!
{% endblock %}

{% block content %}
  <section class="card-shadow">
    <h1>Create a Volunteer Session</h1>
    {{ form_start(form) }}

    <div class="form-group">{{ form_row(form.title) }}</div>
    <div class="form-group">
      <label>De :</label>
      {{ form_row(form.from_date) }}
      {{ form_row(form.from_time) }}
    </div>

    <div class="form-group">
      <label>À :</label>
      {{ form_row(form.to_date) }}
      {{ form_row(form.to_time) }}
    </div>

    <div class="form-group">
      {{ form_row(form.description) }}
      {{ form_row(form.location) }}
      {{ form_row(form.required_volunteers) }}
    </div>

    <button class="button-primary" type="submit">Enregistrer</button>

    {{ form_end(form) }}
  </section>
{% endblock %} #}
